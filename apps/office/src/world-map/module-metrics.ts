/** Mirrored native topology metrics; shared literal vectors detect drift. */
export const MODULE_METRICS = {
  roomWidth: 48,
  roomHeight: 40,
  passageWidth: 8,
  columnStep: 56,
  rowStep: 48,
  lobbyWidth: 104,
  meetingX: 120,
} as const;
